#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t,i;
    cin>>t;
    while(t--)
    {
        long long n,k,p;
        cin>>n>>k;
        set<long long> st;
        map<long long,long long> differences;
        for(i=0;i<n;i++)
        {
            cin>>p;
            st.insert(p);
        }
        for(i=0;i<n;i++)
        {
            
        }
    }
}